import 'package:flutter/material.dart';

class Theaccountscreen extends StatelessWidget {
  const Theaccountscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Scaffold(
        body: Text("there is no design for this screen"),
      ),
    );
  }
}
