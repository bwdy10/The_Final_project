import 'package:flutter/material.dart';

class Theshoppingscreen extends StatelessWidget {
  const Theshoppingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("there is no design for this screen"),
    );
  }
}
